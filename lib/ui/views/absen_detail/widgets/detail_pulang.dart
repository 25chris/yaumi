import 'package:flutter/material.dart';

class DetailPulang extends StatelessWidget {
  const DetailPulang({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
